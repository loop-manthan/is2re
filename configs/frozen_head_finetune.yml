# Frozen-backbone fine-tune: freeze `emb` + `interaction_blocks`, train only the
# 4 output readout blocks at a higher LR.
freeze_backbone: true
lr: 0.0001
epochs: 20
batch_size: 2
warmup_epochs: 2
warmup_factor: 0.2
milestones: [4, 8, 12]
gamma: 0.1
weight_decay: 0.0
patience: 8
seed: 0
