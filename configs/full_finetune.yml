# Full fine-tune: all parameters trainable at a lower LR.
freeze_backbone: false
lr: 0.00003
epochs: 20
batch_size: 2
warmup_epochs: 2
warmup_factor: 0.2
milestones: [4, 8, 12]
gamma: 0.1
weight_decay: 0.0
patience: 8
seed: 0
